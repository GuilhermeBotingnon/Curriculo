const ExecuteFunction = ({ myFunction }) => {
  ExecuteFunction.propTypes;
  return (
    <div>
      <button onClick={myFunction}>Execute the function</button>
    </div>
  );
};

export default ExecuteFunction;
